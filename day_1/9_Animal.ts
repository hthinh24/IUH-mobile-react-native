// 9. Define an interface Animal with name and method sound().

// Khởi tạo interface Animal với từ khóa interface
interface Animal {
    name: string;   // Thuộc tính tên của động vật
    sound(): void;  // Method phát ra tiếng động đặc trưng của động vật
}