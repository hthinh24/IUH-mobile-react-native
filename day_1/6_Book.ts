// 6. Create a class Book with attributes title, author, year.

// Khởi tạo class Book với từ khóa class với 1 attribute
export class Book {
    public title: string;      // Khai báo thuộc tính title kiểu string
    public author: string ;    // Khai báo thuộc tính author kiểu string
    public year: number;       // Khai báo thuộc tính year kiểu number
}